rules:
  - id: set-classname
    message: setClassName
    pattern: $INTENT.setClassName($PACKAGE, $CLASSNAME);
    fix: $INTENT = $INTENT.setClassName($PACKAGE, $CLASSNAME);
    languages:
      - java
    severity: INFO